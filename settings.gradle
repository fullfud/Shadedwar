pluginManagement {
    repositories {
        maven { url = "https://maven.minecraftforge.net" }
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = 'Shadedwar'

include("lattice")
project(":lattice").projectDir = file("lattice")
